<h1>Listing schedule_item_types</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @schedule_item_types.each do |schedule_item_type| %>
  <tr>
    <td><%= schedule_item_type.name %></td>
    <td><%= link_to 'Show', schedule_item_type %></td>
    <td><%= link_to 'Edit', edit_schedule_item_type_path(schedule_item_type) %></td>
    <td><%= link_to 'Destroy', schedule_item_type, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Schedule item type', new_schedule_item_type_path %>

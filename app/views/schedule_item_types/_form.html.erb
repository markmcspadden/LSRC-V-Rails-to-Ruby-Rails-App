<%= form_for(@schedule_item_type) do |f| %>
  <% if @schedule_item_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@schedule_item_type.errors.count, "error") %> prohibited this schedule_item_type from being saved:</h2>

      <ul>
      <% @schedule_item_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

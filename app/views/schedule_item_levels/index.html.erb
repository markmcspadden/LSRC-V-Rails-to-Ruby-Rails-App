<h1>Listing schedule_item_levels</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @schedule_item_levels.each do |schedule_item_level| %>
  <tr>
    <td><%= schedule_item_level.name %></td>
    <td><%= link_to 'Show', schedule_item_level %></td>
    <td><%= link_to 'Edit', edit_schedule_item_level_path(schedule_item_level) %></td>
    <td><%= link_to 'Destroy', schedule_item_level, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Schedule item level', new_schedule_item_level_path %>

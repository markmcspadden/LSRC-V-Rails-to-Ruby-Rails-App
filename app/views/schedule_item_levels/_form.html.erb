<%= form_for(@schedule_item_level) do |f| %>
  <% if @schedule_item_level.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@schedule_item_level.errors.count, "error") %> prohibited this schedule_item_level from being saved:</h2>

      <ul>
      <% @schedule_item_level.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
